# Anymaker 简中汉化包（非官方社区补丁）

利用游戏自带但未启用的官方简中数据（语言表 zh 列 + 简中字体）+ 少量社区短译，
在英文界面下显示中文。**包内不含任何游戏文本**：翻译在安装时从你本机游戏文件推导。

## 安装
1. 完全退出游戏（和暂停中的会话）。
2. **exe 版**：双击运行，菜单选 1，再选路线。**zip 版**：双击 `一键安装.bat`，
   或命令行 `python 安装汉化.py install`（子命令 install/status/uninstall）。
3. 启动游戏验证中文显示。安装器会在自己旁边留 备份_<buildid>\\ 原始文件。

## 两条路线（菜单 1 选择，或 install 后加参数）
- **官方汉化文本路线**（`install official`）：只用游戏自带官方中文译文——语言表 zh 列、
  官方描述表（25480554 起自带全量中文）、官方译名。不含任何社区译文，界面残留英文较多
  （F7 调试面板、5 字节内超短词、生物与僵尸名均为英文），适合想对照官方原文的玩家。
- **润色后路线**（`install polished`，**默认**）：官方中文 + 社区润色与补差——界面短译
  （F7 面板/设置项）、生物与僵尸译名、润色词典三类词条（gcl/name/tsv），界面更完整。
- 两者可随时互相切换：切换时会**先用备份还原官方原始文件再重装**，不会叠加残留。
- 直接跑 `install` 不带参数时：有控制台会询问路线（回车 = 上次所用路线）；供 AI agent /
  脚本静默调用（无控制台交互）时按润色后路线安装。

## 更新
- **每次打开 exe 自动检查** GitHub 上的汉化更新；发现新版本会询问"是否更新词典并重新应用"（词典经 SHA-256 校验，raw.githubusercontent 主源 + jsdelivr 镜像双源）。
- 网络连不上 GitHub 时会**如实提示错误原因**并跳过检查，安装/卸载不受影响。
- 菜单选 4 或 `update` 子命令可手动检查；**exe 本体**的新版本需从 Release 页手动下载替换。

## 卸载 / 回滚
- `python 安装汉化.py uninstall`（按当前版本备份还原）。
- 任何时候可用 Steam『验证文件完整性』彻底还原官方文件。

## 游戏更新后
重新运行 `install` 即可（幂等；gcl 与新文件按内容重新匹配）。

## 润色（改词）
编辑同目录 `润色词典.csv`，保存后重跑 `install`。四类词条：
- `gcl,界面英文原文,中文`——gcl 内嵌串替换，受"等长字节替换"限制（中文字节数不得超过
  原英文，约等于字数上限≈英文字符数÷3），超出自动跳过；
- `name,条目id,中文名`——定义表 JSON name（组件/物品/生物，无长度限制）；
- `tsv,行id或表前缀:行id,译文`——五张基础语言表（lang/comp/item/journal/manual）的
  行，en/zh 双列同改，无长度限制；带前缀（如 `comp:engine`）可限定具体表；
- `desc,行id或cdesc:/idesc:前缀,译文`——两张描述表（组件/物品 tooltip），
  en/zh 双列同改并自动同步 JSON description，无长度限制；支持 `\n` 分段。
润色词典只作用于**润色后路线**；官方汉化文本路线会忽略整个词典。
改名词条建议同时写 `tsv`（语言表）与 `name`（JSON）两条，保持各处一致。

## 已知边界
- MUSIC/MOUSE/ULTRA 等超短词与 F7 调试面板部分条目仍为英文（字节预算不足）——
  这是官方汉化文本路线的预期表现；润色后路线已用社区短译尽量补齐。
- 官方路线另有两处刻意保持英文：生物与僵尸名称、旧版游戏（无官方描述表）的物品描述。
- 已在 buildid 25422107 / 25436400 / 25480554 / 25501161 测试；新版本脚本按内容推导，异常先 uninstall。
- 本补丁与官方未来启用的中文（Steam 语言列表出现简体中文）无冲突：届时 uninstall 后
  在 Steam 属性中直接选择中文即可。
